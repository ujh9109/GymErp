<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ModalMapper">

 
  <!-- ================================
         서비스 상품 선택 모달
    ================================= -->

    <!-- 서비스 상품 목록 조회 -->
    <select id="getServiceModalList" resultType="ServiceDto">
        SELECT
            serviceId,
            codeBid,
            name,
            price,
            isActive
        FROM Service
        WHERE isActive = 1
        <if test="keyword != null and keyword != ''">
            AND name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        ORDER BY
            CASE WHEN codeBid = 'PT' THEN 1 ELSE 2 END,
            name DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 서비스 상품 전체 개수 조회 -->
    <select id="getServiceModalCount" resultType="int">
        SELECT COUNT(*)
        FROM Service
        WHERE isActive = 1
        <if test="keyword != null and keyword != ''">
            AND name LIKE CONCAT('%', #{keyword}, '%')
        </if>
    </select>

    <!-- ================================
         서비스 상품 선택 모달 끝
    ================================= -->
    
    
    
    
    <!-- ================================
         실물 상품 선택 모달 시작 
    ================================= -->
    
    <select id="getProductModalList" resultType="com.example.gymerp.dto.Product"> 
        SELECT
            productId,
            codeBId,
            name,
            price,
            isActive
        FROM product
        WHERE isActive = 1 /* 활성화된 상품만 조회 */
        <if test="keyword != null and keyword != ''">
            AND name LIKE '%' || #{keyword} || '%' /* Oracle 환경을 위한 문자열 연결 */
        </if>
        ORDER BY name DESC
        OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY /* Oracle 12c+ 페이징 */
    </select>

    <select id="getProductModalCount" resultType="int">
        SELECT COUNT(*)
        FROM product
        WHERE isActive = 1
        <if test="keyword != null and keyword != ''">
            AND name LIKE '%' || #{keyword} || '%'
        </if>
    </select>
    
    
    <!-- ================================
         실물 상품 선택 모달 끝 
    ================================= -->    
 
 
</mapper>